// Запитати користувача про кількість кілометрів на день
var kmPerDay = prompt("Скільки кілометрів на день ви проходите пішки?");

// Перевірити умову і вивести відповідне повідомлення
if (kmPerDay <= 3) {
    alert("Мало");
} else {
    alert("Красунчик!");
}
/* Цей код використовує prompt для отримання від користувача кількості кілометрів на день, потім використовує умовний оператор if для перевірки, чи кількість кілометрів менше або дорівнює 3, і виводить відповідне повідомлення за результатами порівняння. */

// Запитати користувача про кількість кілометрів на день
var kmPerDay = prompt("Скільки кілометрів на день ви проходите пішки?");

// Перевірка чи введене значення є числом і більше або рівне нулю
if (!isNaN(kmPerDay) && kmPerDay >= 0) {
    // Перевірка умови і виведення відповідного повідомлення
    if (kmPerDay <= 3) {
        alert("Мало");
    } else {
        alert("Красунчик!");
    }
} else {
    alert("Будь ласка, введіть коректне значення кількості кілометрів на день.");
}
/* Цей код використовує isNaN для перевірки, чи введене значення є числом, і також перевіряє, чи введене число більше або рівне нулю. Якщо введене значення не є числом або менше нуля, виводиться повідомлення про помилку. */

/* Тепер код перевіряє нову умову (чи kmPerDay більше або рівне 20) і виводить "Марафонець!" у відповідних випадках. */
// Запитати користувача про кількість кілометрів на день
var kmPerDay = prompt("Скільки кілометрів на день ви проходите пішки?");

// Перевірка чи введене значення є числом і більше або рівне нулю
if (!isNaN(kmPerDay) && kmPerDay >= 0) {
    // Перевірка умов і виведення відповідного повідомлення
    if (kmPerDay <= 3) {
        alert("Мало");
    } else if (kmPerDay >= 20) {
        alert("Марафонець!");
    } else {
        alert("Красунчик!");
    }
} else {
    alert("Будь ласка, введіть коректне значення кількості кілометрів на день.");
}
/* Якщо ви хочете використовувати && і уникнути ключового слова else, ви можете використовувати дві окремі умови. Ось оновлений код: */
// Запитати користувача про кількість кілометрів на день
var kmPerDay = prompt("Скільки кілометрів на день ви проходите пішки?");

// Перевірка чи введене значення є числом і більше або рівне нулю
if (!isNaN(kmPerDay) && kmPerDay >= 0) {
    // Перевірка умов і виведення відповідного повідомлення
    if (kmPerDay <= 3) {
        alert("Мало");
    }
    
    if (kmPerDay >= 20) {
        alert("Марафонець!");
    }
    
    if (kmPerDay > 3 && kmPerDay < 20) {
        alert("Красунчик!");
    }
} else {
    alert("Будь ласка, введіть коректне значення кількості кілометрів на день.");
}
/* Цей код використовує окремі умови без else для кожного варіанту повідомлення. */

/* Якщо ви хочете використовувати else і уникнути &&, ви можете використовувати окремі блоки if без else для кожного варіанту. Ось оновлений код: */
// Запитати користувача про кількість кілометрів на день
var kmPerDay = prompt("Скільки кілометрів на день ви проходите пішки?");

// Перевірка чи введене значення є числом і більше або рівне нулю
if (!isNaN(kmPerDay) && kmPerDay >= 0) {
    // Перевірка умов і виведення відповідного повідомлення
    if (kmPerDay <= 3) {
        alert("Мало");
    }
    
    if (kmPerDay >= 20) {
        alert("Марафонець!");
    }
    
    if (kmPerDay > 3 && kmPerDay < 20) {
        alert("Красунчик!");
    }
} else {
    alert("Будь ласка, введіть коректне значення кількості кілометрів на день.");
}
/* У цьому коді використовуються окремі блоки if, і кожен з них виконається незалежно від інших. */

/* Якщо ви хочете уникнути як &&, так і else, ви можете використовувати послідовність окремих умов, які будуть виконуватися безпосередньо одна за одною. Ось приклад: */
// Запитати користувача про кількість кілометрів на день
var kmPerDay = prompt("Скільки кілометрів на день ви проходите пішки?");

// Перевірка чи введене значення є числом і більше або рівне нулю
if (!isNaN(kmPerDay) && kmPerDay >= 0) {
    // Перевірка умов і виведення відповідного повідомлення
    if (kmPerDay <= 3) {
        alert("Мало");
    }
    
    if (kmPerDay > 3 && kmPerDay < 20) {
        alert("Красунчик!");
    }
    
    if (kmPerDay >= 20) {
        alert("Марафонець!");
    }
} else {
    alert("Будь ласка, введіть коректне значення кількості кілометрів на день.");
}
/* У цьому коді умови використовуються послідовно, і кожна перевірка буде виконуватися незалежно від інших. */

