// Обміняти змінні місцями
// Створити дві змінні з назвами first та second і покласти в них які-небудь числа.
// Вивести на консоль початкові значення цих змінних
// Написати команди, які обміняють вміст змінних місцями.
// Знову вивести значення змінних, щоб побачити, що обмін дійсно відбувся.
// Увага: алгоритм має працювати однаково з будь-якими числами.
// Створення змінних та присвоєння їм значень
let first = 5;
let second = 10;

// Виведення початкових значень
console.log("Початкові значення:");
console.log("first:", first);
console.log("second:", second);

// Обмін значень змінних місцями
let temp = first;
first = second;
second = temp;

// Виведення значень після обміну
console.log("\nЗначення після обміну:");
console.log("first:", first);
console.log("second:", second);

/* У цьому коді ми створюємо дві змінні first та second, 
присвоюємо їм значення (у цьому випадку, 5 та 10), 
виводимо початкові значення на консоль, обмінюємо значення змінних, 
і виводимо їхні значення після обміну. */

// Не використовувати додаткові змінні
// Створення змінних та присвоєння їм значень
let first = 5;
let second = 10;

// Виведення початкових значень
console.log("Початкові значення:");
console.log("first:", first);
console.log("second:", second);

// Обмін значень змінних місцями без додаткових змінних
first = first + second;
second = first - second;
first = first - second;

// Виведення значень після обміну
console.log("\nЗначення після обміну:");
console.log("first:", first);
console.log("second:", second);

/* У цьому прикладі ми використовуємо арифметичні операції 
(додавання та віднімання) для обміну значень між first та second 
без використання додаткових змінних. */
