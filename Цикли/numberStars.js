// Введіть бажану кількість зірочок
var numberStars = prompt("Введіть кількість зірочок");

// Цикл для виведення зазначеної кількості зірочок в рядок
var resultString = "";
for (var i = 0; i < numberStars; i++){
	resultString += '*';
}

// Виведення рядка зі зірочками на консоль
console.log(resultString);

// У цьому коді ви можете використовувати функцію prompt для введення кількості зірочок, 
// і після цього використовувати цикл для створення рядка з необхідною кількістю зірочок.
// Результат виводиться на консоль за допомогою функції console.log().

// Введіть бажану кількість зірочок
var numberStars = prompt("Введіть кількість зірочок:");

// Перетворення введеного значення у число
numberStars = parseInt(numberStars);

// Перевірка на коректність введення
if (isNaN(numberStars) || numberStars <= 0) {
	console.log("Будь ласка, введіть коректне число більше нуля.");
} else {
	// Ініціалізація лічильника
	var i = 0;
	
	// Рядок для зберігання зірочок
	var resultString = "";
	
	// Цикл while для виведення зазначеної кількості зірочок в рядок
	while (i < numberStars) {
		resultString += '*';
		i++;
	}
	
	// Виведення рядка зі зірочками на консоль
	console.log(numberStars);
}

// У цьому коді використовується цикл while для створення рядка з заданою кількістю зірочок.
// Також введене значення перетворюється у число, і виконується перевірка на коректність введення.